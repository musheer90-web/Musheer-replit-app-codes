## Packages
xlsx | Export data to Excel
jspdf | Generate PDF documents
html2canvas | Capture DOM elements for PDF generation
framer-motion | Smooth animations for UI transitions
lucide-react | Beautiful icons

## Notes
App must be RTL (Right-to-Left) for Arabic language support.
Offline capability simulated via caching; production PWA would need service worker configuration.
Export features require client-side generation.
